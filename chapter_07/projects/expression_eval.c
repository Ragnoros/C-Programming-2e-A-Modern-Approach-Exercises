// Write a program that evaluates an expression.

#include <stdio.h>

int main(void)
{
    float num1, num2;
    char operator;

    printf("Enter the expression: ");

    while (getchar() != EOF && '\n')
    {
    }

    return 0;
}